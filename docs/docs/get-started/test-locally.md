---
sidebar_position: 4
---

# Test locally
You can execute the `textbase_cli test` command in order to test your bot locally. This will spin up an UI at port 4000, so you should be able to navigate to [localhost:4000](localhost:4000) and check if your bot works and interact with it!